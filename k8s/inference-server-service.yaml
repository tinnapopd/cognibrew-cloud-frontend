apiVersion: v1
kind: Service
metadata:
  name: inference-server
  namespace: cognibrew-baseline
  labels:
    app: inference-server
spec:
  type: ClusterIP
  selector:
    app: inference-server
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
